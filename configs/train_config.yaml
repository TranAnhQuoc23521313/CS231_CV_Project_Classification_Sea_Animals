# Training configuration
training:
  batch_size: 32
  epochs: 50
  optimizer:
    type: "Adam"
    lr: 0.001
  scheduler:
    type: "StepLR"
    step_size: 10
    gamma: 0.1
checkpoint:
  save_dir: ../../models/checkpoints
  save_every: 1
